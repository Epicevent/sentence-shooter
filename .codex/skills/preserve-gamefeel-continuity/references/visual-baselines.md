# Visual baseline index

Read the images themselves before relying on these labels.

## Locked evidence

- `../assets/locked/wrong-transition-strip.png`: code-derived reconstruction from the documented v21 palette, minimum lethal speed, round radius, and short trail. It is not a surviving screenshot of the old runtime. It locks same-object continuity and measurable timing evidence, not a permanent ban on other silhouettes.
- `../assets/locked/b-composition.png`: actual current-runtime B composition capture at 1280×720 with seed `20260728`. Treat unapproved torus/heat details inside it as context, not automatically locked mechanics.

## Experimental evidence

- `../assets/experiments/projectile-silhouette-ab.png`: historical legal comparison structure. A retained the round control while B tested an arrow. On 2026-07-29 the user ended that live comparison and selected A's round projectile for both variants. Keep this plate as experiment history, not as authority to restore the B arrow without a new request.

## Capture contract

- Record viewport, seed, build, state, and timestamps beside every capture.
- Capture full frame plus a crop or sprite for the changed effect.
- For motion, use fixed offsets from the same event and include object center positions.
- Replace a locked image only after the user has selected the newer visual direction; otherwise add a dated candidate beside it.
