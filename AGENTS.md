# sentence-shooter — 에이전트 인계 문서 (2026-07-29, v25 통합안 기준)

<!-- CURRENT_CONTRACT_START -->
## 압축·재개 게이트 (항상 가장 먼저 실행)
- 이 저장소 밖의 요약은 이 문서를 대체하지 않는다. 컨텍스트 압축·새 에이전트·작업 재개 뒤에는 코드를 건드리기 전에 저장소 루트에서 `npm run resume`를 실행하고 이 파일 전체를 UTF-8로 다시 읽는다.
- 현재 라이브 계약은 맨 아래의 가장 높은 버전 절이며, 역사 절과 충돌하면 최신 절이 우선한다. 현재 build 표식과 최신 절의 build가 다르면 작업을 완료했다고 말할 수 없다.
- 완료 조건은 `npm test`만이 아니다. 현재 build의 기본 통합안 C에서 독립 실제 플레이 trace 2개가 있어야 하고, 주 개발 에이전트가 분석기 요약이 아닌 두 원본 JSON 전체 시간축을 직접 읽어야 한다.
- 독립 플레이 URL에는 `reviewer=agent-fusion-a` / `reviewer=agent-fusion-b`를 붙인다. `npm run verify:play`는 양쪽 trace에 A/B 두 조립 경로, 제한 요격 보상, 이동 빙설장, 온도를 보존하는 오답 탄막, 실제 피격, 실물 냉각이 모두 있었는지만 기계적으로 확인한다. 통과는 재미 판정이 아니라 검수 증거의 최소조건이다.
- A/B 재미와 게임필은 김록기만 판정한다. 에이전트는 trace 인과, 관측한 조작, 회귀 여부만 보고한다.
- 김록기가 승인한 연출·규칙은 **잠금된 기준선**이다. 사용자가 명시적으로 폐지하거나 재실험 축으로 지정하기 전에는 새 수학·세계관·A/B 작업을 이유로 바꾸지 않는다. 새 요청은 명시된 축만 열며, 인접한 승인 요소를 교체할 권한으로 확대 해석하지 않는다.
- 승인된 오답 탄막 기준선은 노란 열 표현이 같은 위치에서 붉게 무장되고, 속도 반대 방향의 11px 선형 꼬리와 원형 실루엣을 유지한 채 날아오는 v21 렌더다. v25에서 오답은 열장 tracer를 발사 원점으로 표본화할 뿐 권위 온도장과 tracer를 소비·초기화하지 않는다. 화살형을 다시 열거나 공통 색·발생 위치·220ms 예고·속도·잔상, 또는 오답 온도 보존을 바꾸려면 새 사용자 지시가 필요하다.
- 점수 회수의 금색 다이아 실루엣·광량·35ms 간격·약 0.5초 가속 상승·도착별 count-up·마지막 HUD pulse는 build22의 잠금된 공통층이다. 현재 비교축은 도착 구도뿐이다: A는 실제 `score-bank` 중심으로 회수되는 control 복원, B는 기존 `27vw` 고정 도착점을 유지한다. 사용자 판정 전 한쪽을 다른 쪽으로 통일하지 않는다.
<!-- CURRENT_CONTRACT_END -->

김록기(계정 Epicevent)의 개인 프로젝트. TOEFL 2026 Writing의 Build-a-Sentence(어순 재배열) 훈련을
1945/ZType식 슈팅게임으로 만든 것. **이 문서만 읽으면 맥락 없이 이어받을 수 있게 쓰였다.**

## 배포·저장소
- 게임 런타임은 단일 `index.html` (바닐라 JS + Canvas, 의존성 0, 외부 요청 0). `tools/`와 `fixtures/`는 다른 PC의 에이전트 개발 루프를 위한 저장소 내 도구다.
- `main` push = GitHub Pages 자동 배포 → https://epicevent.github.io/sentence-shooter/ (~40초 소요)
- 배포 확인법: `curl -s "https://epicevent.github.io/sentence-shooter/?v=$RANDOM" | grep "<이번 변경의 고유 문자열>"` 을 until 루프로
- gh CLI 인증: Bash에서 `export GH_CONFIG_DIR="/c/Users/com/AppData/Roaming/GitHub CLI"` 선행 필요
- 커밋 정체: `-c user.name="Epicevent" -c user.email="rocky960516@gmail.com"`

## 게임이 지금 이 모양인 이유 (퇴행 금지 이력)
설계는 전부 김록기의 판정+실측 트레이스로 결정됐다. 아래를 모르고 "개선"하면 과거 실패를 재발명한다:

1. **직선탄 타겟 판정 금지 (ZType 원리: 선택=조준).** v2에서 함선이 직선탄을 쏘는 방식을 시도했다가
   같은 열 아래 장갑 단어가 정답을 가려 "맞출 방법이 없는" 참사. 그래서 지금은
   탭/타이핑으로 타깃을 *선택*하면 유도 미사일이 알아서 추적한다. v12의 좌우 함선 조종은
   적탄 회피용이지 정답 타겟을 직선 조준하는 용도가 아니다.
2. **입력은 하이브리드 상시 겸용.** 예전에 TYPE/TAP 모드를 분리했더니 PC에서 마우스로 시작한
   김록기의 키보드가 죽었다(트레이스 meta.mode로 원인 확정). 지금은 키보드+탭이 언제나 둘 다 산다.
3. **후순위 단어의 공짜 상승 금지.** 예전에는 타깃이 아닌 단어가 바닥을 넘으면 위로 리사이클하고
   편대를 +10% 가속했다. 김록기가 "현재 타깃을 고의로 늦춰 미래 단어를 안전한 위쪽으로 보낼 수 있다"는
   지배전략을 발견해 v11에서 폐기했다. 현재 A/B의 후순위 편대는 화면 높이에 따른 바닥 84~144px 안전선에서
   간격 그대로 멈추며 절대
   위로 움직이지 않는다. 한 단어만 움직여 정답을 드러내거나 블록을 관통하지 않도록 화면의 전체 편대는
   함께 멈추지만, 현재 타깃의 논리적 위협거리는 계속 하강·가속한다. 따라서 지연은 미래 단어를 낮은
   위험 위치에 묶고 현재 목숨도 압박한다. 이 압박은 정답 블록을 표시하지 않는 전역 `PRESSURE` 선의
   상승으로 반드시 보여준다. 성공·아이템 비용 없는 후순위 상승을 재도입하지 말 것.
   v13 A/B의 정답 격추 `earned_recoil`은 성공에 대한 명시적 보상이므로 예외다.
4. **아이템 3→2종.** 💡(정답 공개)는 존재했다가 **김록기 판정으로 삭제**: "학습 관점에선 없애는 게 맞아".
   재도입 금지. 현재 A/B 공통: 포커스가 없는 ⚡ Tab = 다음 정답 어구 1개 자동조립,
   포커스가 있는 Tab = 재고를 쓰지 않는 선택 확정,
   시작 4개·최대 6개, 수동 콤보 매 4회마다 +1, PERFECT 문장 +2. 🛡 = 돌파 1회 흡수,
   획득은 스피드 클리어(정답 어구 수×2.8초 이내).
   만땅이면 점수로 전환(OpenTyrian player.c의 consolation-prize 규칙을 실제 소스에서 읽고 이식).
5. **긴 단어 = 고위험 고수익.** HP=철자 수. 트레이스에서 사망 전부가 긴 단어였고 김록기가
   "단어 길면 딜 많이 넣는 게 재밌다"고 판정 → 너프 대신 점수를 길이 비례(60+9×글자)로.
6. **1UP 경제**: 10,000점에 목숨 +1, 이후 +25,000씩 후퇴, cap 5 (OpenTyrian tyrian2.c:1147 규칙 이식).
7. **슈터는 장식이 아니다.** 독립 플레이어가 v11을 `대기 → 문장 전체 해석 → 정적 상자 연속 입력`으로
   평가했고 재플레이 의사가 낮았다. v12는 모든 어구를 즉시 보이고, 모든 살아 있는 블록이
   함선을 향해 실제 적탄을 쏘며, 포인터/좌우 키로 회피하게 한다. 적탄을 추상 압박선만으로
   다시 바꾸지 말 것.

## 트레이스 파이프라인 (이 프로젝트의 학습 기관 — 반드시 유지)
김록기의 지시: "형용사 피드백 말고 실제 쌓인 데이터를 봐라". 게임필은 에이전트가 판정할 수 없다
(우리는 화면을 못 느낀다). 그래서:
- 게임이 세션 전체를 기록: pipeline 5의 8Hz 샘플은 기존 `{t, d(다음단어→데드라인 px), n, q(판정 후 연출 대기), sp, c, l, m}`에
  `scene`을 더한다. `scene.words`는 모든 단어의 렌더/논리 위치·크기·HP·입력 진행·판정/연출 상태·블록/텍스트 가시율·배너 가림률·알파·리코일 잔여시간을,
  `scene.missiles/enemyShots/escortShots/particles/banners`는 아군탄·적탄·편대/카운터탄·잔해·실제 배너 사각형/불투명도를 시간축으로 보존한다.
  `enemyShots`의 `rage/lethal`은 B의 무해한 노란 탄과 오답 뒤 무장된 빨간 탄을 구분하고, `scene.reward`는
  실물 보조 포대 수·카운터 라인 수·탄막 개시 여부·남은 사고 유예시간을 보존한다.
  `scene.recoil_bank`는 A/B 공통 획득·소모 리코일 비축량을 보존한다. `scene.delivery_ms/cargo_order/dock_x`는
  v14 파일 호환을 위한 필드이고 v15 런타임에서는 항상 null이다.
  `scene.jam_ms/move_dir`은 오답 사격 고장 잔여시간과 누르고 있는 회피 방향을,
  `scene.ui`는 실제 상단 조립문·메시지·점수·오버레이 상태를 보존한다. 리코일 비축량 표시는 모바일 블록을 덮는 canvas 오버레이가 아니라 상단 HUD에 둔다.
  `sentence_start`는 문장과 초기 배치를, `formation_recoil`은 리코일 시작점/안전 복귀점/기간을, `shield_absorb.before/after`와 `life_lost.before/after`는
  이동 전후의 전체 장면을 보존한다. 모든 `key_input`/`tap_input`/`tab`/`miss` 이벤트도 그 입력 순간의 전체 `scene`을 가진다. 이 동적 장면 상태를
  요약 필드로 축소하거나 서브에이전트 보고로 대체하지 말 것. 전체 세션 시간축을 읽은 뒤에만 판단할 것.
- 브라우저 높이가 500px 미만이면 게임 시간·입력·충돌을 함께 멈추고 `viewport_pause`를 기록한다. 다시 커지면 단어를 겹치지 않는 안전 편대로 한 번 복귀시킨 뒤
  `viewport_resume`을 기록한다. 각 `viewport` 이벤트에는 전후 크기와 전후 전체 장면을 남기고, `scene.vp`로 프레임별 정지 상태를 남긴다.
  복원 오버레이가 사라지는 첫 프레임부터 모든 텍스트가 완전히 보여야 한다. 분석기는 모든 프레임에서 실제 렌더 사각형의 교차 쌍·면적을 계산해야 하며,
  작은 창에 행 간격을 압축해 겹친 채 목숨을 소모시키지 말 것.
- shield/life/viewport 전체 복귀는 같은 열의 현재 위아래 순서를 보존하고 동일한 단조 진행률로 함께 움직인다.
  후순위 블록은 `wing_hold`에서 편대 전체가 같은 이동량으로 멈춘다. 개별 블록 리사이클·옆 통로·상단 복귀는 v11에서 삭제됐다.
  사용자가 비용을 낸 shield/life 복귀와 뷰포트 복원 외에는 후순위 블록의 y를 감소시키지 말 것.
  이벤트는
  (kill/settle/word_impact/wing_deploy/wing_salvo/wing_shatter/counter_project/counter_reflect/counter_collapse/
  yellow_swarm_arm/enemy_empower/score_gain/score_loss/score_break/miss/input_jammed/miss_suppressed/key_input/type_prefix/
  focus_change/focus_clear/focus_confirm_blocked/focus_confirm/equivalent_swap/wing_hold/earned_recoil/reserve_bonus/
  bullet_clear/ship_hit/life_lost/shield_absorb/clear/tab/item_stock/item_gain/item_overflow/over). `kill`은 논리 판정,
  `settle.lag`는 실제 폭발까지 걸린 ms다. `input_jammed`는 v13~v19 오답 회복 중 차단한 연타의 역사 이벤트이며
  v20 라이브 세션은 0이어야 한다. `miss_suppressed`도 구형 트레이스 호환 이벤트다.
  아이템 이벤트에는 획득 이유와 사용 후 재고가 남으며, `tab`에는 시작 글자 수와 사용 시점의 위협거리도 남는다.
  게임오버 시 `POST /api/trace`. 로컬 수집에서는 1초마다 `POST /api/trace-checkpoint` 증분을 같은 `session_id` 파일에 병합한다. pagehide의 대용량 whole-session keepalive 한도를 넘지 않으며, github.io에서는 체크포인트 루프를 실행하지 않는다.
- 수집 서버는 이 저장소의 `tools/dev-server.js`다. 다른 PC에서도 `npm run dev` 후 `http://127.0.0.1:7777/game`으로 플레이하면
  저장소의 untracked `traces/tr_*.json`에 쌓인다. LAN은 `node tools/dev-server.js --host 0.0.0.0`.
  **github.io에서는 수집 안 됨**(정적)이지만 `tools/`가 함께 커밋되어도 Pages 게임은 계속 동작한다.
- 분석기: `npm run analyze`(최신 로컬 세션), `node tools/analyze-trace.js <파일 또는 --dir DIR>`. 사용자별 절대경로를 넣지 말 것. 출력: 위협거리 분포(긴장 실측),
  킬 템포, 지루 구간, 미스/차단 연타, 문항당 평균·중앙값과 10문항 예상시간, 사망 원인.
- **밸런스 논쟁은 트레이스 숫자로.** "타격감 좋아졌다" 같은 판정 문장을 에이전트가 쓰지 말 것 —
  그 판정은 김록기의 것이다.

## 다른 PC의 새 에이전트 인계 절차
- 이 저장소 하나가 실행 계약이다. 외부 `toefl-writing` 체크아웃을 요구하지 말고 `README.md`의 `npm test` → `npm run dev` 순서로 시작한다.
- 독립 플레이어는 실제 화면 픽셀만 보고 플레이한다. DOM·소스·네트워크·게임 전역·트레이스를 읽는 플레이는 사람형 가독성 검증이 아니다.
- 주 개발 에이전트는 플레이어 보고가 아니라 새로 생긴 게임 소유 `traces/tr_*.json`의 전체 시간축을 직접 읽는다. 분석기 출력은 원본을 대체하지 않는다.
- `fixtures/traces/resize-overlap-before.json`은 반드시 실패해야 하는 음성 대조군이고 `resize-recovery-after.json`은 통과해야 하는 양성 대조군이다.
- 회귀검사 정식 진입점은 `npm test`. 로컬 저장 API, 입력/연출 판정, fixture 기하 분석을 모두 통과시킨 뒤 실제 브라우저 세션을 다시 만든다.
- 브라우저/서브에이전트 기능은 실행 환경의 능력이다. 없으면 blocker로 명시하고, 헤드리스 코드 접근을 독립 플레이로 가장하지 말 것.

## 최신 실측 (2026-07-28, 세션 3개)
- 정확도: 탭 33~43% vs 타이핑 65% — 타이핑이 어순 실수를 줄임
- 미스의 정체: 대부분 "다른 단어를 치기 시작"한 어순 실수. 진짜 철자 실수는 committee 이중자음 1건.
  → **병목은 철자가 아니라 어순** (관사·of-사슬 위치)
- 킬 템포 4.7~5.8초/킬 (ZType 실측 ~1.5초의 3배 느림), 지루 구간 10~16초 존재 → 미해결
- 긴장(위협 150px 이내 체류) 13~16%, 사망 3회/분 — 긴장은 실존하나 템포가 문제

## v10 입력/연출 파이프라인 (퇴행 금지)
- 명중이 결정적인 순간 `resolved=true`, 점수·콤보·`idx`를 즉시 확정한다. 블록은 미사일 충돌과 폭발이
  끝날 때까지 화면에 남고, 그 시점에 `settle`된다. 즉 **판정은 입력을 열고 연출은 뒤따른다.**
- 탭의 전탄은 타깃별 `fireQueues`에 예약된다. 새 타깃을 잡아도 앞 타깃의 예약탄을 취소하거나
  `g.lock`으로 갈아끼우지 말 것. `committed`가 HP 이상이면 그 블록은 이미 죽을 운명이다.
- `resolved` 블록은 클릭 대상·오답·돌파/가속 판정에서 제외한다. 화면에 보인다는 이유로 다시 공격하거나
  플레이어를 해치게 만들면 논리/연출 분리가 깨진다.
- `resolved` 단어 텍스트는 120ms 뒤 사라지고 희미한 X자 잔해만 남는다. 미사일 표적은 보존하되 이미 처리한
  단어가 입력 가능한 블록처럼 보이지 않아야 한다. 이미 판정된 정답의 예약 impact는 문장 전환에서도 정상 점수/실물
  보상을 받고, 입력 보상이 없는 decoy·비정상 잔해만 `settle(forced:true)`로 마감한다.
- 타격감용 50ms hit-stop·폭발·진동·점수 팝업은 `settleWord`에 남는다. 입력 핸들러는 hit-stop 중에도 살아 있다.
- 빠른 성공 세션은 40샘플 미만이어도 이벤트가 있으면 1초 체크포인트에 들어간다. pagehide는 마지막 증분만 `keepalive`로 보내며, 전체 scene JSON을 한 요청으로 다시 보내지 말 것.
- 아래 `miss_suppressed`/시작한 어구만 Tab 규칙은 v10의 역사적 계약이며 현재 A/B에서는 v13이 대체한다.
  키보드에서 한 번 잘못 고른 단어를 빠르게 끝까지 친 경우 220ms 안의 후속 오답은 `miss_suppressed`로만 기록한다.
  벌점·콤보 초기화·대형 lunge는 첫 글자 한 번만 적용하고, 올바른 입력이 들어오면 즉시 새 오답 경계를 연다.
- 키보드 선택은 보이는 모든 미해결 단어의 접두어로 결정한다. 첫 글자가 같은 후보가 여럿이면 `type_prefix` 상태로
  입력을 보류하고, 유일한 후보가 될 때만 잠근다. 오답은 선택한 단어만 붉게 표시하며 현재 정답 블록을 표시하지 않는다.
  이 문단의 옛 Tab 동작과 달리 현재 A/B는 v13 자동조립 규칙을 따른다.
- 회귀검사: `node tools/test-input-pipeline.js`. 연속 Tab 즉시 전진, 화면상 잔존, 헛소모 방지,
  접두어 중의성, 오답 정답노출 방지, 후순위 hold, corpse 오입력 방지, 다중 타깃 탄막 독립성,
  kill→settle 1:1을 고정한다.

## v11 밸런스·선택 수정 (2026-07-28)
- 후순위 단어가 바닥에서 상단으로 무료 리사이클되던 지배전략을 제거했다. 후순위 편대는 144px 안전선에서
  간격을 유지한 채 정지하고, 현재 타깃의 논리적 위협거리만 계속 내려간다. 화면에서는 모든 블록이 같은
  이동량을 유지해 정답을 노출하거나 서로 관통하지 않는다. `wing_recycle` 대신 `wing_hold`를 기록한다.
- 이 v11 Tab 규칙(수동 콤보 5회/식별 후 완성)은 역사 기록이며 현재 A/B에서는 v13이 대체한다.
  현재 값은 시작 4/최대 6, 수동 콤보 4회마다 +1, PERFECT +2다.
- 키보드는 첫 글자로 내부 정답을 직접 고르지 않는다. 같은 접두어 후보는 추가 입력으로 구분하고,
  잘못 고른 단어만 피드백해 현재 정답을 미리 드러내지 않는다. danger 색상과 shield/life 메시지도
  현재 정답 단어를 표시하지 않는다.

## v12 TOEFL A/B 플레이 (2026-07-28, 역사 기록 — v14가 대체)
- 문제 단위를 맥락 없는 50개 완성 문장의 낱말에서, 대화 `A:` + 답변의 고정 선행어구 +
  여러 단어로 된 정답 어구 + 문법적 함정 어구로 바꿘다. 사진 교재 문항은 형식 참고일 뿐이며
  당시 공개 저장소의 12문항은 독자 작성한 문장이었다. v17부터 사용자가 제공한 사진 전사 58문항이 추가됐다.
- `?ab=A&seed=<n>` / `?ab=B&seed=<n>`로 동일한 TOEFL 문항 순서·초기 배치를 짝지어 비교한다.
  두 후보 모두 편대가 하강하고 정답 격추로 `earned_recoil` 시간을 번다. A는 더 조밀한 적탄을
  정답 격추의 범위 패리로 지우고 작은 리코일을 얻으며, B는 적탄이 성기지만 더 큰 리코일을 얻는다.
  학습 요소의 양을 A/B 변수로 쓰지 말 것. 두 후보의 TOEFL 콘텐츠는 항상 같아야 한다.
- 상단 대화와 `B:` 조립 레일은 플레이 중 지속되며, 오답·방어막·목숨 손실은 정답 어구를
  미리 드러내지 않는다. 정답을 모두 조립한 뒤에만 완성 답변을 보여준다.
- 완전히 같은 텍스트의 어구는 시험에서도 교환 가능하므로 같은 토큰으로 판정한다. 포인터로
  다른 복제본을 고른 경우에는 `equivalent_swap`을 기록하고, 타이핑은 동일 후보들을 하나의
  유효 입력으로 취급한다. 좌표 운으로 오답을 만들지 말 것.
- 모든 블록은 2.2초 시작 유예 동안 즉시 화면에 보이고, 이후 살아 있는 블록이 적탄을 쏜다.
  A/B 데스크톱에서는 마우스는 조준/클릭만 담당하고 좌/우 화살표를 누른 동안 함선이 연속 이동한다.
  터치에서는 드래그 이동을 유지한다.

## v13 2차 A/B·동시조작·시험 페이스 (2026-07-28, 역사 기록 — v14가 대체)
- 1차 판정에서 B의 하강/리코일 기반을 채택했지만 최종 단일안으로 굳히지 않는다. 2차 A/B는 그 기반을
  두 후보가 공유한다. 쿼리 없는 `/game`은 편의상 B이며 최종 채택 의미가 아니다.
- Tab은 A/B 모두 시작하지 않은 현재 정답 어구도 1개 자동 조립한다. 시작 4/최대 6, 수동 4킬마다 +1,
  PERFECT +2다. 자동 조립은 콤보를 올리지 않고 PERFECT를 끊는다.
- 오답은 약 570ms부터 누적되는 사격 고장, 편대 30px 이상 하강, 리코일 비축 차감, 2발 이상의
  반격탄을 동시에 만든다. 고장 중 클릭·문자·Tab은 `input_jammed`로 차단하며 만료를 연장하지 않는다.
  정답 어구 완성/새 문항에서 오답 chain을 초기화한다. 오답 연타가 정답 훑기보다 쉬워지면 퇴행이다.
- A/B의 기본 하강은 모두 9.2px/s(상한 11.4)다. 정답 패리와 리코일을 포함한 사고 창은
  둘 다 약 36초/4어구를 목표로 한다.
  이것은 플레이를 6분으로 강제하는 타이머가 아니라 TOEFL 10문항≈6분 사고 여유를 주는 생존 창이다.
  분석기의 실제 완료 문항 `projected 10 min`으로 재조정한다.
- 실제 사용자 B 세션에서 sparse survivor recoil 중 블록 겹침이 관측됐다. 안전 복귀 행 수는
  `ceil(live/perRow)`가 아니라 살아 있는 블록의 최대 row 인덱스로 계산한다.

## v14 3차 A/B·격파 대 운반 (2026-07-28, 역사 기록 — v15가 대체)
- 김록기가 2차 A/B를 "다른점을 모르겠어"라고 기각했다. 패리 범위·리코일·탄속처럼 같은 핵심 루프의
  수치만 바꾼 실험은 더 이상 유효한 A/B가 아니다. 두 후보는 TOEFL 문항과 정답 순서만 같고 성공 동사와
  전장 기하가 즉시 달라야 한다.
- A `BREACH`는 v13 하강 편대 슈터를 계승한다. 정답 어구를 격파하면 주변 적탄을 패리하고 편대를 뒤로 민다.
  오답은 편대를 전진시키고 사격을 고장 내며 반격탄을 만든다.
- B `DELIVERY`는 하강·리코일을 쓰지 않는다. 정답 어구의 철자를 모두 맞히거나 클릭하면 어구가 폭발하지 않고
  함선 위 화물로 포획된다. 함선 위치의 반대편 점멸 도크까지 좌우 이동해 도킹해야 `idx`가 증가한다.
  화물 운반 중 마우스 클릭은 새 어구 선택이 아니라 실제 적탄 요격탄 발사다. 오답은 편대를 움직이지 않고
  약 4.5초 이상의 운반 시간을 태우며 사격 고장과 반격탄을 만든다.
- B는 4어구 기준 46초 운반 창을 가지며 정답 도킹은 최대 2.5초를 회복한다. 시간 만료 시 shield/life를
  소비하고 현재 어구를 다시 시도한다. 이때 포획 직전 남아 있던 `fireQueues`와 `missiles`를 반드시 제거해야
  리셋된 현재 정답이 연출탄에 삭제되어 진행 불능이 되지 않는다.
- Tab 자동조립, 접두어 중성 판정, 정답 비노출, 동일 텍스트 교환, 키보드 이동+마우스 동시 입력은 두 게임의
  공통 계약이다. 같은 seed는 문항 순서를 고정하지만 전장 배치까지 같게 만들지 않는다.

## v15 4차 A/B·같은 게임, 다른 B풍 디자인 (2026-07-28)
- 김록기는 A의 BREACH 게임성을 선택했고 B의 시각 디자인을 선호했다. 따라서 비교 중인 두 안은 더 이상
  성공 동사가 다른 게임이 아니다. 속도·적탄·정답 패리·편대 리코일·오답 lunge/jam/반격·보상은 완전히 같다.
- A `VECTOR GRID`는 청록 직선 레일, 3열 전술 편대, 수평 pressure line으로 읽힌다.
  B `SIGNAL CORE`는 보라/청록 중앙 코어, 후보-코어 방사형 연결선, 2열 편대와 원형 pressure arc로 읽힌다.
  모바일 B의 코어는 좁은 1열 어구와 겹치지 않도록 편대 아래의 작은 코어로 이동한다.
- 키보드는 즉시 사격하지 않는다. `w`처럼 여러 어구가 일치하면 모든 후보를 `FOCUS`로 표시하고,
  추가 글자로 고유해지면 `TAB` 표시를 낸다. Tab 확정 시에만 정오를 판정한다. `ww`처럼 일치 후보가
  사라지면 포커스만 해제하며 miss·반격·재고 소모가 없다. 복수 후보 상태의 Tab도 판정을 보류한다.
- 포커스가 있는 Tab은 무료 확정이다. 포커스가 없는 Tab만 ⚡를 소비해 현재 정답 어구를 자동 조립한다.
  마우스 클릭은 기존처럼 즉시 선택·판정하며 화살표 이동과 동시에 가능하다.
- 같은 seed의 A/B는 TOEFL 문항과 정답 순서뿐 아니라 게임 밸런스도 같고, 전장 기하와 시각 정보 구조만 다르다.
  `delivery_ms`, `cargo_order`, `dock_x`는 과거 trace 호환을 위한 nullable schema 필드로만 남으며 런타임에서는 null이다.

## v16 최종 B·실물 명중·회피 코어 (2026-07-28)
- 김록기가 B `SIGNAL CORE`를 최종 기반으로 채택했다. 쿼리 없는 `/game`은 B이며 A는 이전 비교안 확인용이다.
- 정답 입력 시 `resolved`, 점수, 콤보, `idx`, 다음 어구 개방은 즉시 처리한다. 그러나 B의 블록은 투명해지거나 X표가
  생기지 않으며 HP도 그대로다. 예약 미사일이 캔버스상의 실제 `wordVisualX/Y` 경계에 접촉할 때만 HP가 줄고,
  마지막 HP가 0이 된 바로 그 프레임에 `settleWord`·폭발·사운드·히트스톱·점수 팝업이 발생한다.
- 마지막 어구의 논리 판정만으로 `sentenceClear`하지 않는다. `pendingSentenceClear`로 보류하고 마지막 실제 충돌 뒤에
  decoy 정리·적탄 제거·700ms 다음 문항 전환을 시작한다. 전환에 의해 강제 정리된 잔탄은 `forced:true`로 구분한다.
- B 적탄이 함선 피격 반경 밖 `GRAZE_RADIUS`에 들어와 후보 상태가 된 뒤, 함선의 피격 띠 아래로 안전하게 빠져나가면
  탄마다 한 번만 `GRAZE +20`을 얻어 SYNC를 최대 100까지 채운다. 링 진입 즉시 보상하면 안 된다. 피격 시 SYNC 40을
  잃는다. 따라서 가만히 맞거나 같은 탄을 왕복해 충전할 수 없어야 한다.
- SYNC 100에서 **수동으로 판정한 단어의 실제 마지막 피격**이 들어오면 코어가 충전을 소비해 전탄 패리, +250,
  이중 확산 링을 만든다. Tab 자동 조립(`word.auto`)은 일반 impact parry만 만들고 SYNC를 소비하지 않는다.
- 추적 이벤트는 `graze`, `impact_parry`, `core_burst`, `settle.impact_cleared/core_burst`와 scene의 `sync`, `pulses`,
  `enemyShots[].grazed/graze_armed`를 남긴다. 논리 kill과 물리 settle의 1:1, 코어 중복 소비, 접촉 전 조기 사망을 회귀 테스트한다.

## v17 사진 교재 58문항 확장·배포 (2026-07-28)
- 사용자가 대화에 제공한 교재 사진 6장의 모든 58문항을 전사해 기존 12문항 뒤에 추가했다. 총 `ITEMS.length`는 70이다.
- 사진의 인쇄 고정 중간어구는 현재 `lead/answer/tail` 구조에서 완성문장이 동일하도록 인접 answer chunk에 합쳤다.
  게임 화면은 원문의 A 질문과 B 완성문장을 보존하되, 너무 많은 한 단어 블록으로 모바일 전장을 깨지 않도록 3~4개의
  의미 단위 청크로 묶는다. 사진에 남는 오답 형태가 있는 문항은 decoy로 보존한다.
- 사진 문항은 `source:'photo-…'`와 안정적인 `photo-*` id를 갖고 `sentence_start.source`에도 기록된다. 회귀 테스트는
  원본 12 + 사진 58 = 70, 사진 source 58, 고유 id 70을 고정한다.

## v18 물리 반사·BULLET WIPE·실제 총기 티어 (2026-07-28)
- 오답 확정의 편대 즉시 전진(lunge)과 적탄 즉시 생성은 폐기한다. 오답 판정·점수 차감·콤보 초기화·jam은 입력 즉시
  확정하지만, 화면에서는 플레이어 탄 한 발이 선택한 오답 블록까지 날아가 실제 접촉한 순간만 붉게 변하고 반사된다.
- 반사탄은 접촉 순간의 함선 좌표를 한 번만 조준해 직진한다. 이후 함선을 추적하면 회피 입력이 무의미해지므로 금지한다.
  피하면 화면 밖에서 소멸하고, 맞으면 `takeShipHit`의 shield/life/SYNC 손실을 정확히 한 번 적용한다.
- `SYNC 100` 수동 마지막 명중은 사용자 문구와 화면 모두 `BULLET WIPE`다. 적탄은 판정 프레임에 즉시 무해화하되,
  삭제 직전 좌표를 `coreAbsorbs`에 복제해 340~560ms 동안 곡선 궤적으로 신호 코어에 빨려 들어가는 연출을 남긴다.
  Tab 자동 조립은 이 충전을 소비하지 않고 일반 `IMPACT CLEAR`만 만든다.
- 총기 레벨은 콤보 3/6/10에 MK-II/III/IV로 상승한다. 레벨 1~4의 미사일 속도는 630/700/770/840px/s,
  전탄 큐 간격은 90/75/60/45ms, 레벨별 기본 명중 삭제 반경은 185/209/233/257px다. 여기에 콤보당 4px,
  최대 48px를 더한다. 레벨업 비교는 콤보 증가 전 `weaponBefore`와 증가 후 `weaponAfter`로 해야 한다.
- trace scene에는 `ricochets`, `coreAbsorbs`, 미사일 `weapon_level/speed`를 포함한다. 사건은 `wrong_shot`,
  `shot_reflect`, `ricochet_hit`, `bullet_wipe_visual`, `weapon_up`을 남긴다. 회귀 테스트는 편대 무이동, 반사 조준 고정,
  회피 가능, 실제 피격 1회, 전탄 흡수 개수, 레벨별 실제 수치를 검증한다.

## v19 반사 물리 A/B 재개 (2026-07-28)
- 김록기는 단일 반사 해석을 확정하지 않고 다시 A/B를 요구했다. 두 안은 모두 채택된 `SIGNAL CORE`의 2열 방사형
  전장, TOEFL 문항, SYNC, GRAZE, BULLET WIPE, 총기 티어 수치를 공유한다. 구형 VECTOR 렌더러는 역사 코드일 뿐
  라이브 `ab=A/B`에서 사용하지 않는다. 따라서 같은 seed의 문항과 최초 블록 기하는 두 안이 같아야 한다.
- A `snap`: 오답탄 접촉 순간의 현재 함선 좌표를 한 번 잠그고 분홍 점선으로 직진한다. 플레이어는 반사를 본 뒤
  이동해 피한다. B `backtrack`: 오답탄이 저장한 `originX/Y`의 발사점으로 주황 역궤적을 직진한다. 플레이어는
  탄이 오답에 닿기 전부터 원래 사격선을 비울 수 있다. 어느 안도 반사 후 추적 유도를 사용하지 않는다.
- 적대 상태는 `enemyShots`와 `ricochets.phase==='return'`의 합이다. 전탄 제거를 표방하는 BULLET WIPE와 문항 clear는
  둘 다 제거해야 하며, 삭제 좌표 모두 `coreAbsorbs`에 포함한다. 반사탄 종료는 `ricochet_end.reason`의 `hit`,
  `dodged`, `bullet_wipe`, `impact_clear`, `wave_clear`, `ship_reset`, `wave_transition`으로 기록한다.
- 분석기의 hostile avg/max에는 return ricochet을 포함한다. 회피율은 `shot_reflect - ricochet_end` 추정이 아니라
  종료 사유를 직접 센다. 회귀 테스트는 A aim=접촉 시 ship, B aim=발사 origin, 실제 dt 화면 이탈 회피, 정지 피격,
  wipe의 반환탄 흡수, MK-III/IV 실제 큐 시점과 실제 충돌 반경을 검증한다.

## v20 보상 루프 A/B·즉시 재입력·5/4 정지 수정 (2026-07-29)
- 김록기가 v19의 두 안을 모두 기각했다. 반사 목적지만 다른 A/B는 방향성 피드백을 만들지 못했다. 최근 A 5세션
  98.3초에서 오답 29회 중 후속 입력 17회가 JAM에 막혔고, 조작 불쾌감이 물리 패널티보다 먼저 발생했다.
  따라서 라이브 A/B에서 오답 JAM은 0이며, 오답 직후 클릭·문자·Tab이 즉시 다시 동작해야 한다.
- 오답 반사 연출은 둘 다 유지한다. 오답 블록 접촉 시점의 함선 좌표를 한 번 잠가 직진하고 회피해도 보상은 없다.
  둘의 반사 물리를 다르게 두지 않는다. 대신 오답은 0점에서 공짜가 되지 않도록 점수를 음수까지 차감하며,
  A는 콤보/총기, B는 FLOW를 함께 잃는다.
- A `CHAIN CANNON`은 정확성 공격 게임이다. 수동 콤보 3/6/10에서 MK-II/III/IV, 기존 630~840px/s와
  90~45ms 전탄 간격, 185~257px+콤보 충돌 삭제 반경을 사용한다. 오답 기본 점수 손실은 180이고 연속 오답마다 +60이다.
- B `DODGE CORE`는 이동 방어 게임이다. 총기는 MK-I로 고정된다. 일반 적탄을 안전하게 graze한 뒤에만 FLOW 1칸과
  +30을 얻으며, 3칸에서 다음 수동 정답의 실제 마지막 명중이 전탄 삭제와 +300을 만든다. 반사탄은 FLOW를 주지 않는다.
  `g.combo`는 항상 0이고 콤보 점수 배율·총기 성장·4킬 Tab 보상을 받지 않는다. 오답 기본 손실은 120,
  연속 오답마다 +50이며 FLOW를 전부 잃는다. 피격도 FLOW를 전부 잃는다.
- 오답 return shot은 A의 총기 단계를 상속하지 않는 공통 MK-I 물리다. 두 안의 outbound 속도·반경과 return
  속도·반경이 동일해야 하며, 이를 테스트가 숫자로 고정한다.
- 초기 적탄 사망이 사고 시간을 압도하지 않도록 공통 wave grace는 4초다. A는 더 성기고 빠른 적탄
  (2.1초/155px/s), B는 더 촘촘하고 느린 graze용 적탄(1.3초/128px/s)을 쓴다. 블록 기본 하강은 8px/s다.
- 실측 B에서 완성 표시가 `5/4`가 된 뒤 새 문항이 나오지 않았다. 마지막 정답을 논리 판정한 뒤 실제 충돌을 기다리는
  동안 order 4 decoy가 `idx===order`를 만족해 다섯 번째 정답처럼 처리됐고, 최종 settle 조건 `idx===4`를 영구히
  놓친 것이 원인이다. `resolveWord`는 decoy와 `idx>=sentence.length`를 거부하고, final-impact 대기 중 선택 입력을
  잠그되 이동은 계속 허용한다. 회귀 테스트가 관측된 `5/4` 상태를 직접 금지한다.

## 열려 있는 후보들 (지시 아님 — 김록기 판정 필요)
## v21 실물 보상·노란 탄 부채·점수 회수 A/B (2026-07-29)
- v20 사용자 세션 A는 84초 동안 정답 24/오답 22로 6문항을 통과했다. `weapon_up` 5회가 있었지만 성공 동사는
  계속 같은 유도탄이었고, 정답 `impact_parry` 시점에는 지울 탄이 거의 없었다. 최소 적탄 거리가 457px라 회피도
  쉬웠다. B는 첫 문장을 2초에 완성했지만 FLOW 0 상태로 다음 문장에서 피격 종료했다. 따라서 수치/게이지만 다른
  A/B와 적탄이 없는 패리는 폐기한다. 기존 A 전체를 폐기하는 뜻이 아니라 CHAIN CANNON의 공격 성장 축을 실물화한다.
- A `CHAIN CANNON`은 수동 정답의 **실제 settle**에서 보조 포대를 최대 3기 도킹한다. `weaponLv`는 숨은 combo가
  아니라 `wingUnits` 0~3에서 MK-I~IV가 되고, 이후 미사일은 함선+실제 포대 원점을 순환한다. 만충 뒤 정답은
  `wing_salvo`로 화면의 적탄을 요격한다. 각 도킹은 함선 반경 245px의 급박한 적탄만 `FORMATION COVER`로
  걷어내 보상을 읽을 시간을 만든다. 오답/실제 선체 손상은 포대를 파괴하지만 실드 방어는 포대를 보존한다.
  combo는 점수/Tab 리듬에만 남고
  총기 단계의 진실 원천이 아니다.
- B `COUNTER FIELD`는 A의 방어형 숫자 적립판이 아니다. 수동 정답 settle 위치 아래에 8.5초·3충전짜리 세로 라인을
  최대 2개 만들며 플레이어가 라인으로 직접 이동해야 탄을 반사한다. Tab 자동 조립은 3.4초·1충전의 짧은 라인이다.
  일반 B 적탄은 `lethal:false`인 느린 노란 탄으로 함선을 통과한다. 라인에서 반사하면 +35로 환전된다. 폭 변경 때
  활성 라인의 x도 새 viewport 비율로 이동·clamp하고, 마지막 충전/시간 만료 뒤 HUD와 canvas 라인 수를 함께 0으로 만든다.
- B 오답 순간 모든 라인을 무너뜨리고, 화면에 이미 있는 노란 탄을 현재 함선 위치로 재조준해 반경 6.5 이상·
  302px/s 이상 빨간 탄(`lethal:true`)으로 무장한다. 적립 숫자가 아니라 화면에 남겨 둔 노란 탄 수가 오답 부채다.
  오답 리코셰가 선택 적에게 물리 접촉하면 그 적은 RAGE 1~3이 되고 이후 탄은 빨간 고속탄이다. 단어 한 칸 점프와
  오답 JAM은 다시 넣지 않는다.
- 영어 판단 시간과 순수 게임 난도를 같은 속도로 묶지 않는다. 단어 하강은 4초 뒤 기존 연속 가속을 유지하지만,
  탄막은 첫 답안 확정 또는 12초 뒤 시작한다. A 적탄은 처음부터 치명적이고, B는 안전 자원을 오답 때 치명적 탄막으로
  뒤집는 별도 긴장 구조다.
- 점수는 정답 논리 판정이 아니라 실제 settle에서 획득한다. `scoreMotes`는 폭발 위치에서 HUD로 가속 이동하고 도착할
  때마다 표시 점수를 올린다. 다음 1,500점 `SCORE BREAK`를 항상 표시하고, 문턱 통과는 전탄 충격파와 빈 실드 1칸을
  준다. 오답 점수는 0 아래로 내리지 않는다. 숫자를 없애는 것이 아니라 목표·회수·문턱·실물 효용을 붙인다.
- 마지막 어구의 투사체가 앞 어구보다 먼저 도착해도 문장을 지우지 않는다. 논리 정답 어구의 예약 impact가 전부 정상
  settle된 마지막 순간에만 `sentenceClear`를 한 번 호출해 느린 앞 탄의 점수·포대·라인 보상을 보존한다.
- 게임 사례 연구는 설명/스크린샷만 보지 않는다. 오픈소스 구현에서 새 점수 이벤트가 기존 tween을 죽인 뒤 다시
  pulse하는 방식, 목표 문구를 FIRST TO/MATCH POINT처럼 상태에 따라 바꾸는 방식, 픽업이 target을 향해 가속하고
  실제 도착 콜백에서 점수를 증가시키는 방식을 코드까지 확인한다. 본 게임도 중첩 CSS pulse 재시작, score mote
  도착별 count-up, 항상 보이는 BREAK 문턱으로 회귀 테스트한다.

## 열려 있는 후보들 (지시 아님 — 김록기 판정 필요)
- ZType에 레코더 주입해 그의 실제 ZType 플레이 분포와 직접 diff
- 추가 문항을 넣을 때는 대화 맥락·완성문장·source와 고유 id를 함께 유지한다. 별도 TOEFL 랩은 필요하지 않다.

## 김록기와 일하는 법 (이 프로젝트에서 실제로 관측된 것)
- 게임필·Canvas 연출·A/B·속도·밀도·타격감·보상 시각화를 건드리는 작업은 저장소의
  `.codex/skills/preserve-gamefeel-continuity/SKILL.md`를 먼저 읽고 따른다. 문자 설명만으로 이전 감각을
  재구성하지 말고 스킬의 PNG 골든마스터를 직접 확인한다.
- 시각 결정은 도형 하나가 아니라 실루엣·광량·잔상·속도·밀도·화면 구도·입력 반응이 결속된 기준선이다.
  실험은 강하게 해도 되지만 A/B 양쪽에서 그 기준선을 설명 없이 동시에 교체해 비교점을 없애지 않는다.
- 원형→화살 사건의 최초 지적은 모양 금지가 아니라 승인 기준선을 설명 없이 두 후보에서 동시에 교체한 것이었다.
  이후 합법적인 B 실험으로 분리했지만, 김록기가 이번 v24에서 비교 종료와 A 원형안 통일을 선택했다. 따라서 현재 라이브에는
  화살을 남기지 않되, 과거 피드백을 처음부터 화살형 자체의 영구 금지였다고 왜곡하지 않는다.
- 사용자의 질문·관찰·아이디어·선택·변경 지시를 구분한다. 좁은 질문에는 그 대상만 먼저 답하고,
  사용자가 오해를 바로잡으면 기존 확대 해석을 즉시 폐기한다. 누적된 불만을 임의로 중지·전면 리셋·수습 요청으로
  재해석하지 않는다.
- 다른 작업에 보내는 메시지는 반드시 `에이전트 작성 인계 메모`라고 표시한다. 에이전트의 해석을 사용자의 새 지시처럼
  쓰지 말고, 단순 맥락 전달인지 구현 요청인지 구분한다.
- 피드백은 짧다("노잼", "에바", "긴장감 없어"). 원인 진단은 에이전트 몫. 진단을 그에게 되묻지 말 것.
- 그는 실물 관찰을 요구한다("실제하는 게임을 보고 배워", "게임은 본질적으로 동적"). 코드 정독만으로
  게임성 결론을 내리면 지적받는다. 헤드리스 실측(엔진 전역 접근, 오토플레이 계측)이 통했던 방법.
- 그의 직관이 기존 설계보다 나으면 그대로 채택한 전례("콤보 쌓으면 tab?" → 콤보12=⚡ 채택).
- 검증 없이 "됐다"고 보고하지 말 것. 이 게임의 검증 패턴:
  ```js
  // 헤드리스에서 물리 수동 펌프 (rAF가 스로틀되므로)
  startGame('type'); W=800; H=600; nextSentence(); g.speed=0;
  const t=g.words.find(w=>w.order===0); t.y=180;
  for (const ch of t.text) if (/[a-z0-9'-]/i.test(ch)) handleKey(ch);
  let fr=0; while(g.idx===0&&fr<900){ update(0.016); fr++; }  // fr<900이면 킬 성공
  ```
  주의: 창이 화면에 없으면 canvas가 0×0이라 W/H를 수동 주입해야 한다.

## 관련 프로젝트
- TOEFL 랩은 문제 출제·채점을 위한 선택적 별도 프로젝트다. Sentence Shooter의 플레이·수집·분석·배포 인계에는 필요하지 않다.

## v23 토러스 열장·오답 상전이·A/B 보상 (2026-07-29, 이전 열/탄 규칙을 대체)
- 화면은 2차원 토러스 전체다. `y=0`은 천장과 바닥이 동일시되는 원이며 `T=0` 절대영도 Dirichlet 선이다.
  초기조건은 전 영역 `T=0`이고, 시작 시 보이는 노란 tracer도 0개여야 한다.
- 권위 상태는 64×40 `Float32Array` 온도장이다. `dt=1/30`, `κ=.00125`로
  `∂tT=κΔT-C_local*T`를 명시적 유한차분하며, 이중 버퍼를 재사용한다. 전역 냉각은 금지한다.
  180개 노란 점은 온도장을 표본화하는 시각 tracer일 뿐 상호작용 물질점이 아니다. 계산은 `O(grid+tracer+raster cells)`여야 하며
  PDE 셀 루프 안에서 모든 냉각 노드를 순회하면 안 된다. 브라우저 trace의 step/max microsecond를 보고 C/WASM 여부를 결정한다.
- 단어 상자는 유일한 열원이며 움직이는 고정온도 경계다.
  `T_word=min(1,.25+.55*descent+.10*rage)`. 논리 정답 뒤에도 마지막 미사일이 실제로 박힐 때까지 경계를 유지한다.
- 오답은 선택 상자의 rage를 먼저 올린 뒤, 현재 노란 열 tracer를 220ms 예고 붉은 화살로 전환하고 온도장을 0으로 비운다.
  붉은 화살은 소멸 뒤 열로 돌아오지 않는다. 각 수명은 `arm + distance/speed + 1.25s` 이상이어야 하며 먼 탄이
  조준점 전에 고정 수명으로 사라지면 안 된다. 선체 피격은 충돌한 한 발만 소비하고 나머지 volley를 보존한다.
  따라서 밀집 오답은 i-frame 뒤의 먼 탄이 이어져 실제 게임오버를 낼 수 있어야 한다. 단어 상자 직접 발사,
  일반 적탄, 오답 리코셰는 라이브 코드에 없어야 한다.
- 정답은 남은 단어 편대를 한 칸 밀거나 순간이동시키지 않는다. A/B 공통 판정·문항·초기 배치는 같고 보상 동사만 다르다.
- A `torus_interceptor`: 실제 word impact가 보조포를 최대 3기 도킹한다. 붉은 탄막 중 함선+포대가 실제 요격탄을 쏜다.
  선체 피격은 포대 한 기만 손상한다.
- B `quench_wake`: 실제 word impact combo가 cooler RPM을 정한다. 이동할 때만 원형 wake node를 남긴다.
  노드는 국소 sink이자 붉은 화살 감속·편향장이다. 사각 safe zone은 금지한다. 실제 좌표 변화가 없으면 node를 만들 수 없고,
  인접 node는 refresh하여 벽 적층을 금지한다. sink 총합은 explicit-Euler 양성 조건 안에서 포화해야 한다.
  오답은 RPM만 0으로 만들고 기존 node는 즉시 지우지 않는다.
- `torus-23` build 표식을 HUD와 trace meta에 모두 남긴다. A/B는 사용자가 승인하는 절차가 아니라 직접 플레이하고 계속
  피드백하기 위한 비교안이다. 코드 테스트만으로 재미를 통과했다고 선언하지 않는다.

## v24 감각 기준선 보존·대형 호위기 대 빙설장 A/B (2026-07-29, v23 A/B 보상·화면 표현을 대체)
- 권위 상태와 수학은 v23의 64×40 열장, 30Hz explicit stencil, 절대영도 seam을 유지한다. 180개 tracer는 오답 상전이용
  내부 표본으로만 남고 평상시 화면에는 개별 노란 점으로 그리지 않는다. 화면은 같은 격자를 연속 amber 열안개로 보간한다.
- 오답은 tracer가 표본화한 실제 hot fog 위치에서 붉은 위협을 응축하고, 같은 색·광량·속도·220ms 예고·11px 꼬리를 공유한다.
  tracer는 열장의 표본이지 피해량 단위가 아니므로 한 오답의 붉은 volley는 화면 6×4 구획의 고온 대표를 우선한 최대 36발이다.
  장시간 숙고로 active tracer가 36개를 넘더라도 탄 수가 무제한 증가하지 않으며, trace는 eligible/cap/suppressed 수를 남긴다.
  김록기가 화살 실험을 종료했으므로 A/B 모두 v21 원형 실루엣을 쓴다. 라이브 렌더에 variant별 화살 polygon이나
  `RED ARROWS` 표기를 남기지 않는다. trace의 역사 호환 event/field 이름 `heat_arrow_*`는 schema 이름일 뿐 화면 형태가 아니다.
- A의 실물 보상은 플레이어+대형 호위기 최대 1기다. 호위기 한 기에 3개의 실제 포구를 보이므로 도킹 뒤 전체 발사 원점은
  플레이어 1+호위기 3=4개다. 다수 소형 함대의 화면 혼잡은 없애되 기존 최대 화력량과 붉은 탄 자동 요격 루프는 유지한다.
- B의 word impact는 cooler RPM과 넓은 장의 반경만 충전하며 그 자리에서 wake node를 만들지 않는다. 실제 좌우 이동 때만
  node를 만들되, 짧은 방향키 nudge도 좌표가 변했다면 반드시 즉시 movement node를 만든다. keydown과 keyup 사이에 animation
  frame이 없었다는 이유로 이동만 되고 보상이 누락되면 안 된다.
  반경 `148+18*rpm`px, 약 24초 수명의 원형 빙설장을 최대 5개 남긴다. 빙설장은 넓은 반투명 등온장과 회전 고리/눈보라로
  전체 반경이 읽혀야 하고, 국소 sink를 강하게 적용하며 내부의 붉은 원형탄을 빠르게 감속·편향·quench한다.
- 이동체 충돌은 탄과 선체 양쪽의 프레임 전후 좌표를 상대운동으로 swept 검사한다. 한 발 피격 뒤 나머지 volley는 i-frame에
  닿았다는 이유로 삭제하지 않고 잠시 대기한 뒤 같은 고정 조준선에서 재개한다. 플레이어가 이동하면 피할 수 있지만 가만히
  있으면 세 번의 실제 `heat_arrow_hit`/`ship_hit`로 게임오버까지 이어질 수 있어야 한다.
- 점수 회수 효과는 A/B 공통으로 build22의 회전 금색 다이아, 도착별 점수 증가, 마지막 숫자 pulse를 유지한다. A의 다이아는
  현재 HUD의 실제 `score-bank` 중심으로 올라가고, B는 기존 화면폭 27% 고정점을 유지해 점수바 결속감 대 독립 상승감을 비교한다.
  `score_gain.target_mode/target_x/target_y`로 실제 도착점을 기록한다.
- 상단 HUD는 플레이 결정을 돕는 A/B 표식·목숨·점수/BREAK·현재 문장 결합 진행·A ESCORT 또는 B STORM 자원·실제 VOLLEY
  경보만 보인다. build/best/FLOOR 수치, `FLOOR COOL`, `YELLOW HARMLESS`, `ROUND CONTROL/ARROW EXPERIMENT`와 전장·하단의
  중복 조립문은 플레이 HUD에서 제거한다. build는 trace와 코드 상수로 유지한다.
- 논리 정답 판정은 다음 입력을 즉시 열지만 어구를 B문장에 미리 붙이지 않는다. 실제 마지막 미사일 명중과 settle에서만
  `assembly_launch`를 만들고, A `heavy_interceptor_rail_slam`은 폭발 위치→문장 레일을 380ms 직선 도킹하며,
  B `blizzard_core_link`는 폭발 위치→신호 코어→문장 청크 체인을 560ms에 연결한다. `assembly_dock` 뒤에만
  `visualAttached`와 HUD 진행 수가 증가한다. 빠른 다중 입력은 독립 flight를 보존하며 마지막 clear의 700ms 안에 도킹한다.
- trace meta의 build는 `torus-24`, pipeline은 6이다. `scene.heatArrows`는 silhouette/chill/source 좌표를,
  `scene.wakeFields`는 각 빙설장의 위치·반경·수명·RPM·접촉 수를, `scene.assemblyFlights`는 route/source/target/age/duration을
  남긴다. `npm run resume`는 AGENTS.md 전체와 build 일치를,
  `npm run verify:play`는 current-build `agent-a`/`agent-b` 원본에서 실물 보상·상전이·실제 피격의 최소 증거를 확인한다.

## v25 레일·빙설 통합과 성공 냉각 경제 (2026-07-29, v24 보상 분기를 기본 통합안으로 대체)
- 쿼리가 없는 `/game`과 `?ab=C`는 기본 통합안 `THERMAL FUSION`이다. `?ab=A/B`는 승인된 두 연출의 비교 기준을 재현하는
  보조 경로로 남긴다. C의 정답 청크는 짝수 order에서 A의 380ms `direct_rail_slam`, 홀수 order에서 B의 560ms
  `core_link`를 그대로 사용한다. 두 연출의 실루엣·색·경로·도킹 타이밍을 새로운 혼합 애니메이션으로 덮지 않는다.
- C의 실제 word impact는 대형 호위기와 STORM을 함께 충전한다. 대형 호위기의 3포구 실루엣은 유지하지만 무한 자동요격은
  금지한다. 실물 정답 명중 1회당 `escortAmmo` 1발, 최대 3발만 벌며 붉은 탄 1발을 실제로 요격할 때 1발을 소비한다.
  C의 정답 미사일 성능은 MK-I로 고정하고, word impact의 직선 냉각파도 1개만 발사한다. A 비교 경로의 4포구 연출은 보존한다.
- C는 B의 movement-only 빙설장을 그대로 가진다. 정답 실물 명중 콤보가 `coolerLevel`을 충전하고 실제 좌우 좌표 변화가
  있을 때만 넓은 원형 장을 남긴다. 독립 미사일이 논리 순서와 반대로 settle되어도 후착한 낮은 combo가 이미 번 RPM을
  되감지 못한다. C의 냉각 래스터는 직선 pressure wave와 원형 wake field를 둘 다 계산한다.
- 오답은 현재 연속 열안개의 tracer를 최대 36개 표본화해 같은 위치에서 원형 붉은 탄을 만든다. 이때 `field.temp`,
  `field.next`, `floorBins`, `totalMass`, tracer의 active/armed 상태는 바꾸지 않는다. 이벤트는 `heat_volley_armed`이며
  `field_unchanged:true`와 발사 직전 `heat_integral`을 남긴다. 실패를 이용해 세계 열을 빼는 전략은 성립하면 안 된다.
- 성공한 냉각만 권위 온도장을 바꾼다. 문장 clear는 C에서 직선 rail과 방사형 quench를 함께 발동하며 방사 front가 처음
  통과한 각 격자 셀의 온도를 52% 줄인다. 1,500점 `SCORE BREAK`는 현재 붉은 탄막을 지우고 88% 방사 냉각과 넓은 rail을
  함께 발동한다. 살아 있는 단어 경계는 다음 stencil에서 고정온도 열원으로 다시 clamp된다. 따라서 시각 파동, 안개 감소,
  HUD 온도, 실제 격자 변화가 같은 시간축에서 움직인다.
- trace meta build는 `torus-25`, pipeline은 7이다. `scene.quenchBursts`는 id/중심/현재·최대 반경/수명/강도/reason/통과 셀 수를
  보존한다. `thermal_clear_start/end`는 `heat_before/after`를, `escort_intercept_spent`는 남은 탄약을 남긴다. 실제 초기 열량이
  0.1 미만이어도 0으로 뭉개지지 않도록 heat integral/floor/max/source 값은 trace에서 소수 4자리로 보존한다.
  기본 C 완료 검증은 `reviewer=agent-fusion-a`와 `reviewer=agent-fusion-b`의 두 원본 세션을 주 개발 에이전트가 끝까지 읽고,
  `npm run verify:play`가 두 조립 경로·제한 요격·이동 빙설·온도 보존 오답·실제 피격·실물 냉각을 확인해야 한다.
